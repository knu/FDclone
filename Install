[FDclone をインストールする法]

1. machine.h でコメントアウトされている部分を、付属のコメント
   文を参照しながら、自分のマシン環境に合わせて、行頭の "/*"
   を削除。
   SunOS, NEWS-OS, HP-UX, IRIX では特に変更の必要はありません。

2. fd.h で定義されている定数の幾つかを、自分の好みに合わせて
   変更。各定数の意味は、マニュアルに書かれている環境変数の項
   を参照のこと。
   環境変数名と全く同じ定数名にしてあります。

3. make。

4. コンパイルされたものが無事に動くことを確認。

5. Makefile.in にある BINDIR, MANDIR を自分の環境に応じて書き
   換える。

6. make install。

7. 終了。
